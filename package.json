{
  "name": "basicprotopackage",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:proto:win": "rm -rf src && mkdir src && protoc --plugin=./node_modules/.bin/protoc-gen-ts_proto.cmd --ts_proto_out=src ./*.proto ",
    "build:proto:lin": "rm -rf src && mkdir src && protoc --plugin=./node_modules/.bin/protoc-gen-ts_proto --ts_proto_out=src ./*.proto"
  },
  "author": "Krzysztof Tomkow",
  "license": "MIT",
  "devDependencies": {
    "ts-proto": "^1.139.0",
    "typescript": "^4.9.5"
  }
}
